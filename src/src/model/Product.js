class Product {
    constructor(name, tag, description, imageUrl, price) {
        this.name = name;
        this.tag = tag;
        this.description = description;
        this.imageUrl = imageUrl;
        this.price = price;
    }
}

// Exporting the Product class
export default Product;